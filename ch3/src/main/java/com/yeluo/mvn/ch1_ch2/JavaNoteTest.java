package com.yeluo.mvn.ch1_ch2;
/**
 * 
 * @author YeLuo
 *
 */
/*java的注意点
 *  1.Java对大小写敏感的
 *  
 *  2.关键字public称为访问修饰符：用于控制程序的其他部分对这段代码的访问级别。
 *  
 *  3.关键字class表明Java程序中的全部内容都包含在类中。只需要将类作为一个加载程序逻辑的容器，程序逻辑定义了应用程序的行为。
 *  Java应用程序中的全部内容都必须放置在类中。
 *  4.class后面紧跟类名，类名的命名规则是字母开头（大写），后面可以跟子母和数字的任意组合（驼峰法）。但是不能使用Java保留字。
 *  
 *  5.Java保留字：abstract  assert  boolean  break  byte  case  catch  char  class  const  continue 
 *  default  do  double  else  enum  extends  final  float  for  goto  if  implements  import  
 *  instanceof  int  interface  long  native  new  null  package  private  protected  public 
 *  return  short  static  strictfp  super  switch  synchronized  super switch  this  throw  
 *  throws  transient  try  void  volatile  while 
 *  分类    基本数据类型：byte char short int long float double boolean enum void
 *  	 控制关键字：if  else  do  while for  goto  switch case default  break  continue  return
 *      异常处理关键字：try  catch  finally  throw  throws  
 *      访问修饰符：public protect default private
 *      常见的：class package interface import extends implements final native new  null static 
 *      super this  synchronized   
 *      少见的：assert  strictfp  transient volatile
 *    
 *   6.源代码文件名必须和公共类名一样。如果没有公共类名，则没有硬性要求。
 *   
 *   7.为了代码能过执行，在类的源文件中必须包含一个main方法，而且main方法必须声明为public static void。
 *   
 *   8.用花括号划分程序的各部分（通常称为块）。
 *   
 *   9.Java的所有函数都属于某个类的方法（标准术语称为方法，而不是成员函数），因此Java中的main方法必须是有一个外壳类。
 *   
 *   10.Java中语句以分号结束。
 *   
 *   11.Java的通用语法：object.method（parameters）等价于函数调用。
 *   
 *   12.Java与C/C++一样，采用双引号分隔字符串。
 *   
 *   13.注释（3种）
 *   警告：在Java中，第二种和第三种注释 不能嵌套。
 *   第一种 // ：其注释内容从//到本行结束。
 *   第二种/*长篇注释*/
/*   第三种/**可以用来自动生成文档*/
   

public class JavaNoteTest {

}
